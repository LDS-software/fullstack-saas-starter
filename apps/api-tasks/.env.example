# Database configuration for local Docker container
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=""
DIRECT_URL=""

APP_URL=http://localhost:3000

# Example of local configuration using Mailtrap or Mailgun/Resend in production
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USER=
MAIL_PASS=
MAIL_FROM=do-not-reply@your-app.com
